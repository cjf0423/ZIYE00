##  ziye 
## github地址 https://github.com/6Svip120apk69
## TG频道地址  https://t.me/ziyescript
## TG交流群   https://t.me/joinchat/AAAAAE7XHm-q1-7Np-tF3g
## boxjs链接  https://raw.githubusercontent.com/6Svip120apk69/gitee_q8qsTAUA_cThxc1RBVUE/main/Task/ziye.boxjs.json


## 新建一个库，此文件路径为 .github/workflows/ziye-sync.yml 打开教程链接 https://www.jianshu.com/p/bb82b3ad1d11 ，申请PAT（勾选时尽量全部勾选 ），找到Settings------secrets------New repository secret------name填写PAT    value填写PAT的值        

## 点击 Actions  手动运行或者等待定时运行即可自动生成分支 ZIYE 且自动同步文件至此

## 其他作者库同理  替换如下    
 
## 作者名         6Svip120apk69  
## 作者库名       gitee_q8qsTAUA_cThxc1RBVUE     
## 作者分支名     main     
## 同步分支名     ZIYE   （可自定义 ）




name: shaolin-kongfu-sync  #作者名-sync
on:
  schedule:
    - cron: '32 4 */10 * *'  #时间
  workflow_dispatch:
  watch:
    types: started
  repository_dispatch:
    types: sync-shaolin-kongfu-js_scripts  #要改sync-作者名-仓库名
jobs:
  repo-sync:
    env:
      PAT: ${{ secrets.PAT }} 
    runs-on: ubuntu-latest
    if: github.event.repository.owner.id == github.event.sender.id
    steps:
      - uses: actions/checkout@v2
        with:
          persist-credentials: false

      - name: sync shaolin-kongfu-js_scripts  #要改sync-作者名-仓库名
        uses: repo-sync/github-sync@v2
        if: env.PAT
        with:
          source_repo: "https://github.com/shaolin-kongfu/js_scripts.git"  #要改GitHub地址.git
          source_branch: "main"  #要改
          destination_branch: "shaolin-kongfu"  #名字
          github_token: ${{ secrets.PAT }}
